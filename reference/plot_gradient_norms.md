# Function to plot the gradients norms.

The gradient norms of the parameters per iteration, computed for each
input `K` used during model selection.

## Usage

``` r
plot_gradient_norms(x)
```

## Arguments

- x:

  a mvnmm object.

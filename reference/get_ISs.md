# Get the number of ISs per cluster.

Get the number of ISs per cluster.

## Usage

``` r
get_ISs(x, highlight = c())
```

## Arguments

- x:

  the fitted object

- highlight:

  the clusters to retrieve

## Value

an array with as names the clusters in `highlight` and as values the
number of ISs assigned to each cluster

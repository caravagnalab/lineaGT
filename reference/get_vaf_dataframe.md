# Retrieve the mutations dataframe.

Function to retrieve the mutations dataframe used to initialize the
object.

## Usage

``` r
get_vaf_dataframe(x, verbose = T)
```

## Arguments

- x:

  a mvnmm object.

## Value

the mutations dataset.

## Examples

``` r
if (FALSE) get_vaf_dataframe(x)
```

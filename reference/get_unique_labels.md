# Extract the list of unique observations labels.

Returns a list with `K` elements, corresponding to the unique labels.

## Usage

``` r
get_unique_labels(x, init = FALSE)
```

## Arguments

- x:

  a mvnmm object.

## Value

list of unique labels.

## Examples

``` r
if (FALSE) get_unique_labels(x)
```

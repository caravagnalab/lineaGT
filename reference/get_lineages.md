# Extract the data lineages.

Returns a vector with the lineages of the input data.

## Usage

``` r
get_lineages(x, verbose = T)
```

## Arguments

- x:

  a mvnmm object.

## Value

vector of data lineages.

## Examples

``` r
if (FALSE) get_lineages(x)
```

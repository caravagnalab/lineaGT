# Barplot of the per-cluster mixture weights and number of ISs.

add

## Usage

``` r
plot_mixture_weights(x, vcn = NULL)
```

## Arguments

- x:

  a mvnmm object.

## Examples

``` r
if (FALSE) plot_mixture_weights(x)
```

# Extract the model dimensions.

Returns a vector with the dimensions of the model.

## Usage

``` r
get_dimensions(x, verbose = T)
```

## Arguments

- x:

  a mvnmm object.

## Value

vector of model dimensions.

## Examples

``` r
if (FALSE) get_dimensions(x)
```

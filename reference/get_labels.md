# Extract the observations labels.

Returns a list with `N` elements, corresponding to the labels for each
observation.

## Usage

``` r
get_labels(x, init = F)
```

## Arguments

- x:

  a mvnmm object.

## Value

list of observations labels.

## Examples

``` r
if (FALSE) get_labels(x)
```

# Extract the estimated mixing proportions.

Returns a list of dimension `K` with the estimated mixing proportions.

## Usage

``` r
get_weights(x)
```

## Arguments

- x:

  a mvnmm object.

## Value

list of estimated mixing proportions.

## Examples

``` r
if (FALSE) get_weights(x)
```

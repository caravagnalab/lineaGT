# Function to plot the training losses.

Function to plot the training losses.

## Usage

``` r
plot_losses(x, train = FALSE)
```

## Arguments

- x:

  a mvnmm object.

- train:

  Boolean. If set to `TRUE`, the losses computed in the model selection
  are visualized.

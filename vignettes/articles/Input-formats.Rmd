---
title: "Input formats"
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r include=FALSE}
library(reticulate)
conda_create("r-reticulate")
conda_install("r-reticulate", "pylineaGT", pip=TRUE)
```

```{r setup}
library(lineaGT)
library(magrittr)
```

The dataset can be filtered calling the `filter_dataset()` function.

```{r}
data_filt = data %>% filter_dataset()

data_filt
```

